$('#my-gossips').append('<%= j render @gossips %>');
<% if @gossips.next_page %>
  $('.pagination').replaceWith('<%= j will_paginate @gossips %>');
<% else %>
  $(window).off('scroll');
  $('.pagination').remove();
<% end %>