<!DOCTYPE html>
<html>

<head>
  <% content_for :title, "Gossip Detail"%>
</head>

<body>

  <div class="container mt-4">
    <h4>Potin details</h4>

    <div class="row">
      <div class="card border-success mb-3" style="max-width: 18rem;">
        <div class="card-header">
          <%= link_to "Author: " + @gossip.user.first_name , user_path(:first_name => @gossip.user.first_name)%></p>
        </div>
        <div class="card-body">
          <p class="card-text">Id: <%=  @gossip.id %></p>
          <p class="card-text">Title: <%=  @gossip.title  %></p>
          <p class="card-text">Content: <%=  @gossip.content %></p>
          <p class="card-text">Created date: <%=  @gossip.created_at %></p>
        </div>
      </div>
    </div>
  </div>
</body>

</html>